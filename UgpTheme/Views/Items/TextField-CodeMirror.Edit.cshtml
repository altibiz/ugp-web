@using Members.PartFieldSettings
@model OrchardCore.ContentFields.ViewModels.EditTextFieldViewModel

@{
    var settings = Model.PartFieldDefinition.GetSettings<TextFieldSettings>();
    var culture = await Orchard.GetContentCultureAsync(Model.Field.ContentItem);
    var settingsExt = (FieldSettingsExt)(Model as IShape).Properties["FieldSettingsExt"];
}

<div class="form-floating">
    @if (settingsExt.Disabled)
    {
        <textarea disabled="disabled" asp-for="Text" placeholder="smth" class="form-control content-preview-text" dir="@culture.GetLanguageDirection()" style="height: 100px"></textarea>
    }
    else
    {
       <textarea asp-for="Text" placeholder="smth"  class="form-control content-preview-text" dir="@culture.GetLanguageDirection()" style="height: 100px"></textarea>
    }
    @if (!String.IsNullOrEmpty(settings.Hint))
    {
        <span class="hint">@settings.Hint</span>
    }
    <label asp-for="Text">@settingsExt.Label</label>
</div>
